plugins {
    id 'groovy'
    id 'maven'
    id 'signing'
    id "com.gradle.plugin-publish" version "0.12.0"
}

apply plugin: 'groovy'

def env = System.getProperty("profile") ?: "maven"

apply from: "profile-${env}.gradle"

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}